# php-apache-mysqli-docker
The simplest docker setup for php mysqli apache project
### Build the Docker Image:
```http
docker-compose build
```
### Bring Up the Services:

```http
docker-compose up
```
